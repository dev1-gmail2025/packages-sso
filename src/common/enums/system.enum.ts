export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  DELETED = 'DELETED',
  PENDING = 'PENDING',
}

export enum Mode {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum Environment {
  PRODUCTION_VPS = 'production_vps',
  DEVELOP = 'develop',
  STAGING = 'staging',
  PRODUCTION = 'production',
}
